<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="UsingSQLite.Views.MainPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    Title="{Binding Title}">

    <StackLayout HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand">
        <Button Command="{Binding ViewListFlowerTypeCommand}" Text="Xem danh sách loại hoa" />
        <Button Command="{Binding ViewListFlowerCommand}" Text="Xem danh sách hoa" />
    </StackLayout>

</ContentPage>